import { AnydocPreviewClient } from "@/components/debug/AnydocPreviewClient";

export const metadata = {
  title: "AnyDoc Preview Lab — Hidden",
};

export default function AnydocPreviewPage() {
  return <AnydocPreviewClient />;
}
